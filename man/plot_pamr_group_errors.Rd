% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pamr_group_errors.R
\name{plot_pamr_group_errors}
\alias{plot_pamr_group_errors}
\title{Plot misclassification error by group for a PAMR model}
\usage{
plot_pamr_group_errors(
  pamr_model,
  pamr_data,
  cv_results,
  pal = "Set1",
  base = 18,
  legend_position = "right"
)
}
\arguments{
\item{pamr_model}{A PAMR model object produced by \code{pamr.train()}.}

\item{pamr_data}{A list containing \code{x} (expression matrix) and \code{y} (class labels),
typically the same structure used by \code{pamr.train()}.}

\item{cv_results}{Cross‑validation results from \code{pamr.cv()}.}

\item{pal}{Either a ColorBrewer palette name (e.g. "Set1") or a custom vector of colors.}

\item{base}{Base font size for the ggplot theme.}

\item{legend_position}{Position of the legend. One of:
\code{"right"}, \code{"left"}, \code{"top"}, \code{"bottom"}, \code{"none"}.}
}
\value{
A ggplot object.
}
\description{
Generates a line plot showing the misclassification error per group
across different PAMR thresholds (i.e., number of genes used).
}
\examples{
\dontrun{
data(d)
library(pamr)
pamr_model <- pamr.train(d)
cv_results <- pamr.cv(pamr_model, d)
plot_pamr_group_errors(pamr_model, d, cv_results, pal = "Set1", legend_position = "bottom")
}

}
